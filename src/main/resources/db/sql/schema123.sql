CREATE TABLE SeatHold (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY ( START WITH 1,
  INCREMENT BY 1
) PRIMARY KEY,
  email VARCHAR (50
),
  status VARCHAR (10
),
  creation_ts TIMESTAMP
);

CREATE TABLE Seat (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY ( START WITH 1,
  INCREMENT BY 1
) PRIMARY KEY,
  row_id INTEGER,
  col_id INTEGER,
  level_id INTEGER,
  status VARCHAR (10
),
  seathold_id INTEGER
);

